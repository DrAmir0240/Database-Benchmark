<div dir="rtl">

# 📊 مقایسه عملکرد MongoDB و PostgreSQL

این پروژه نتایج تست‌های عملکرد بین **MongoDB** و **PostgreSQL** را در هشت نوع کوئری مختلف نشان می‌دهد.  
هر بخش شامل میانگین زمان اجرا و تحلیل مقایسه‌ای بین دو سیستم است.

---

## 🏗️ ساختار تست

- همه تست‌ها روی **سرور یکسان** با شرایط مشابه اجرا شده‌اند.
- **اندیس‌گذاری مناسب** در هر دو سیستم برای هر کوئری فراهم شده است.
- هر تست چندین بار اجرا شده و **میانگین زمان‌ها محاسبه شده** است.

---

## 📈 نتایج و تحلیل

### 1. Q1 — PRIMARY KEY READ

| سیستم      | میانگین زمان | p95      |
|------------|--------------|----------|
| MongoDB    | 0.228 ms     | 0.340 ms |
| PostgreSQL | 0.084 ms     | 0.101 ms |

**تحلیل:**  
PostgreSQL حدود ۲–۳ برابر سریع‌تر از MongoDB در جستجوی کلید اولیه است. دلیل: استفاده از **B-Tree اندیس بهینه** و عملیات
در حافظه، در مقابل سربار BSON و موتور aggregation در MongoDB.

---

### 2. Q2 — INDEXED FILTER (username)

| سیستم      | میانگین زمان |
|------------|--------------|
| MongoDB    | 0.248 ms     |
| PostgreSQL | 0.084 ms     |

**تحلیل:**  
با وجود اندیس روی username در هر دو سیستم، PostgreSQL سریع‌تر است. MongoDB به دلیل **سربار پارس کوئری و تبدیل BSON**
کندتر عمل می‌کند.

---

### 3. Q3 — RELATION / JOIN

| سیستم      | میانگین زمان |
|------------|--------------|
| MongoDB    | 0.344 ms     |
| PostgreSQL | 0.074 ms     |

**تحلیل:**

- MongoDB از `$lookup` استفاده کرده که مانند join در حافظه انجام می‌شود و برای حجم‌های بزرگ بهینه نیست.
- PostgreSQL join را با **اندیس روی کلید خارجی** بهینه اجرا می‌کند.

---

### 4. Q4 — AGGREGATION

| سیستم      | میانگین زمان |
|------------|--------------|
| MongoDB    | 0.265 ms     |
| PostgreSQL | 0.087 ms     |

**تحلیل:**  
PostgreSQL سریع‌تر است، اما MongoDB pipeline aggregation برای عملیات ساده مانند **count و group by** قابل قبول است.

---

### 5. Q5 — RANGE QUERY

| سیستم      | میانگین زمان | حداکثر زمان |
|------------|--------------|-------------|
| MongoDB    | 1.214 ms     | -           |
| PostgreSQL | 1.569 ms     | 8.653 ms    |

**تحلیل:**  
MongoDB در این تست بهتر عمل کرده است، احتمالاً به دلیل استفاده بهینه از **اندیس روی createdAt**. در PostgreSQL ممکن است
full scan یا partial index به درستی استفاده نشده باشد.

---

### 6. Q6 — INSERT

| سیستم      | میانگین زمان |
|------------|--------------|
| MongoDB    | 0.218 ms     |
| PostgreSQL | 0.099 ms     |

**تحلیل:**  
PostgreSQL با auto-commit و **WAL بهینه**، سرعت insert بالاتری دارد. MongoDB سربار تولید `_id` و serialization دارد.

---

### 7. Q7 — UPDATE

| سیستم      | میانگین زمان |
|------------|--------------|
| MongoDB    | 0.199 ms     |
| PostgreSQL | 0.090 ms     |

**تحلیل:**  
PostgreSQL با **page cache و WAL** سرعت بهتری در عملیات update دارد.

---

### 8. Q8 — TRANSACTION

| سیستم      | میانگین زمان |
|------------|--------------|
| MongoDB    | 7.616 ms     |
| PostgreSQL | 0.683 ms     |

**تحلیل:**  
اختلاف بسیار زیاد (حدود **۱۰ برابر**) به نفع PostgreSQL است. دلیل: MongoDB از **سربار عملیات دو مرحله‌ای و oplog**
استفاده می‌کند، در حالی که PostgreSQL با **row-level locking** بهینه اجرا می‌شود.

---

## ⚖️ جمع‌بندی نهایی

| نوع کوئری      | MongoDB     | PostgreSQL | تحلیل                              |
|----------------|-------------|------------|------------------------------------|
| PK Read        | کندتر       | سریع‌تر    | PostgreSQL بهتر                    |
| Indexed Filter | کندتر       | سریع‌تر    | PostgreSQL بهتر                    |
| Join/Lookup    | کندتر       | سریع‌تر    | PostgreSQL بهتر                    |
| Aggregation    | نزدیک       | سریع‌تر    | PostgreSQL بهتر، MongoDB قابل قبول |
| Range Query    | سریع‌تر     | کندتر      | MongoDB بهتر (وابسته به اندیس)     |
| Insert         | کندتر       | سریع‌تر    | PostgreSQL بهتر                    |
| Update         | کندتر       | سریع‌تر    | PostgreSQL بهتر                    |
| Transaction    | بسیار کندتر | سریع‌تر    | PostgreSQL بسیار بهتر              |

---

## ✅ نکات مثبت

- پیاده‌سازی هر کوئری در هر دو سیستم با **منطق مشابه** انجام شده است.
- **اندیس‌گذاری مناسب** در هر دو سیستم در نظر گرفته شده است.

## ⚠️ نکات منفی

- MongoDB به طور ذاتی برای عملیات **JOIN** طراحی نشده و `$lookup` سربار دارد.
- Transaction در MongoDB از نسخه‌های جدید پشتیبانی می‌شود، اما همچنان نسبت به PostgreSQL کندتر است.
- در **range query** ممکن است اندیس در PostgreSQL بهینه‌سازی نشده باشد.

---

---

## 🎯 نتیجه‌گیری تخصصی برای CRM

### ۱. ماهیت سیستم

- CRM معمولاً حجم زیادی داده‌ی کاربر، کسب‌وکار، تراکنش و فعالیت‌ها دارد.
- بسیاری از کوئری‌ها **join-heavy** و **aggregation-heavy** هستند.
- نیازمند **transaction atomicity** است (مثلاً تغییر اطلاعات کاربر یا ثبت تراکنش همزمان در چند جدول).
- بنابراین workload کاملاً **OLTP** است، نه صرفاً document-read-heavy.

### ۲. تحلیل عملکرد دیتابیس‌ها روی workload شما

| جنبه                         | MongoDB                                                         | PostgreSQL                                                   |
|------------------------------|-----------------------------------------------------------------|--------------------------------------------------------------|
| Primary Key / Indexed Lookup | خوب، ولی overhead BSON و driver باعث کمی کندی می‌شود            | عالی، B-Tree index بسیار بهینه، latency کم                   |
| Join / Relation              | `$lookup` کند و memory-heavy، scale کردن join پیچیده است        | native join با index فوق‌العاده سریع، مناسب رابطه‌های پیچیده |
| Aggregation / Grouping       | pipeline overhead دارد، اگر ساده باشد قابل قبول است             | group by با index بهینه و native SQL سریع‌تر                 |
| Transaction Multi-Table      | کند (۷–۱۰ برابر Postgres در تست شما)، overhead two-phase commit | بسیار سریع و بهینه، atomic و safe                            |
| Insert / Update              | مناسب document-heavy، ولی برای relational خیلی بهینه نیست       | سریع، با WAL و page cache، بهینه برای update-heavy CRM       |
| Range Query / Reports        | خوب اگر index درست باشد                                         | با index درست سریع ولی در partial index ممکن است کند شود     |

### ۳. نتیجه‌گیری نهایی

- اگر CRM شما **heavy relation / transaction / reporting** دارد (مثل اکثر CRMهای واقعی):  
  ✅ **PostgreSQL گزینه بهینه و منطقی است.**
    - تمام queryهای join، aggregation و transaction با Postgres خیلی سریع و پایدار هستند.
    - برای داده‌های ۱۰M رکوردی یا بیشتر، Postgres scaling منطقی و predictable دارد.

- **MongoDB فقط زمانی مناسب است که:**
    - داده mostly document-oriented باشد و join / transaction کم باشد.
    - بیشتر read-heavy و schema-flexible باشد (مثلاً collection‌های متنوع و غیرساختاری).

- در سیستم CRM واقعی، MongoDB باعث می‌شود:
    - عملیات join یا transaction سنگین کند باشد.
    - latency بالا برای کاربران و گزارشات real-time ایجاد شود.
    - پیچیدگی pipeline برای aggregation و lookup زیاد شود.

💡 **خلاصه یک خطی:**
> برای CRM سنگین و داده‌های relational، PostgreSQL انتخاب امن و بهینه است؛ MongoDB صرفاً در حالت read-heavy و
> document-oriented می‌تواند جایگزین باشد، ولی برای سیستم واقعی CRM که join و transaction زیاد دارد، عملکرد آن کافی نیست و
> باعث bottleneck می‌شود.
</div>